# تشغيل مشروع تسعير الطباعة بنظام PHP

## المتطلبات

- PHP 7.4 أو أحدث.
- إضافة PHP القياسية `json` و`session` و`hash`.
- صلاحية كتابة داخل مجلد `data` وملفات `values.json` و`papers.json` و`settings.json`.

## التشغيل المحلي

من داخل مجلد المشروع:

```bash
php -S localhost:8000 router.php
```

ثم افتح:

```text
http://localhost:8000
```

يجب عدم فتح ملفات HTML مباشرة من القرص، لأن تسجيل الدخول والحفظ يحتاجان خادم PHP.

## Apache

ضع المشروع داخل مجلد الموقع. ملف `data/.htaccess` يمنع تنزيل بيانات الحسابات مباشرة. تأكد أن Apache يسمح بقراءة ملفات `.htaccess` وأن PHP يستطيع الكتابة داخل `data` وملفات JSON الخاصة بالأسعار.

## بيانات التطبيق

- بيانات الحسابات ومحاولات الدخول: داخل `data` وهي محمية.
- الأسعار والإعدادات: `values.json` و`papers.json` و`settings.json`.
- جميع عمليات الإدارة تمر عبر `server.php` وتتطلب جلسة مدير.

## فحص سريع

1. افتح الصفحة الرئيسية وسجل الدخول.
2. تأكد من ظهور لوحة المدير لحساب المدير فقط.
3. عدّل قيمة صغيرة واحفظها.
4. أعد تحميل الصفحة وتأكد من بقاء القيمة.
5. سجّل الخروج وتأكد من منع صفحات الإدارة من الحفظ.
